# varius 저장소 공통 가이드

> 이 저장소는 여러 잡다한 프로젝트를 모아놓은 모노레포다. 상위의 PHP/CodeIgniter 4 중심
> 공통 규칙은 이 저장소 전체에 강제하지 않으며, 이 문서의 저장소 규칙을 우선 적용한다.

## 저장소 구조

- 프로젝트마다 루트에 서브디렉터리를 만들어 격리한다. 예: `rtic/`.
- 각 서브디렉터리는 자체 `AGENTS.md`와 `README.md`를 반드시 보유한다.
  - `AGENTS.md`: 스택, 아키텍처, 개발 및 검증 명령어 등 Codex 작업 규칙
  - `README.md`: 사람이 읽는 프로젝트 개요와 실행 방법
- 새 프로젝트를 시작할 때는 위 두 파일부터 만든다.

## 이슈 라벨과 작업 범위

- GitHub 이슈에는 대상 서브디렉터리명과 같은 라벨을 붙인다. 예: `rtic/` 관련 이슈는 `rtic` 라벨.
- 이슈 작업에 착수할 때는 이 라벨을 작업 대상 디렉터리로 삼는다. 해당 범위 밖 파일은 변경하지 않는다.
- 저장소에 라벨이 없으면 이슈 생성 전에 `gh label create <서브디렉터리명>`으로 만든다.
- 여러 프로젝트에 걸친 이슈 또는 루트 `AGENTS.md`·`README.md` 같은 공통 변경에는 라벨을 생략할 수 있다.

## 언어와 프로젝트 규칙

- 프로젝트별 언어는 PHP, Node.js, Python, Go 등으로 다를 수 있다. 특정 언어의 전제를 저장소 전체에 강제하지 않는다.
- 언어, 프레임워크, 코드 스타일 규칙은 해당 프로젝트의 `AGENTS.md`가 정의한다.

## Git 워크플로우

이 저장소는 PR 없이 직접 머지한다.

```
feature/* → dev → main
```

- 기능 작업은 `feature/*`에서 완료한 뒤 PR 없이 `dev`에 직접 머지한다.
- `dev`가 배포 가능한 상태가 되면 `main`에 직접 머지한다.
- 브랜치 전략은 유지하되, 리뷰 절차만 생략한다.
- 커밋 메시지는 `이모지 + Conventional Commits 접두어 + 한국어 설명` 형식을 사용한다.

## 검증

- 이 저장소에는 CI/CD 파이프라인이 없다.
- 테스트, 정적 분석, 빌드 확인은 머지 전에 로컬에서 수행한다.
- 각 프로젝트의 구체적인 검증 명령은 해당 프로젝트의 `AGENTS.md`에 명시한다.

## 문서 전용 변경

- `*.md` 문서만 수정하는 경우에는 `dev`에 직접 커밋할 수 있다.
- 코드가 한 줄이라도 포함되면 `feature/*`에서 작업한 후 `dev`로 머지한다.
